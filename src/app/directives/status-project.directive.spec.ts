import { StatusProjectDirective } from './status-project.directive';

describe('StatusProjectDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusProjectDirective();
    expect(directive).toBeTruthy();
  });
});
